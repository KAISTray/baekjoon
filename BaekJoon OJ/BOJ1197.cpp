#include <stdio.h>
#include <stdlib.h>
#include <vector>
#include <tuple>
#include <algorithm>

using namespace std;

int main() {
	int v, e;
	scanf("%d %d", &v, &e);
	
	vector<tuple<int, int, int> > edge;
	
	for (int i=0; i<e; i++) {
		int init,fin,d;
		scanf("%d %d %d", &init, &fin, &d);
		
	}
}
